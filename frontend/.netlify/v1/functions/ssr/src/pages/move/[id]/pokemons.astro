---
import { PokemonsPage } from "src/components/move/pokemons/PokemonsPage.tsx"
import Layout from "src/layouts/Layout.astro"

export const prerender = false

const { id } = Astro.params
---
<Layout title={`${id} - Pokemons`}>
  {id &&
    <PokemonsPage client:load idMove={id} />
  }
</Layout>